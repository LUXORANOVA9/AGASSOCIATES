name: Check Runner
on: workflow_dispatch
jobs:
  check:
    runs-on: self-hosted
    steps:
      - run: |
          echo "USER: $(whoami)"
          echo "HOME: $HOME"
          echo "USERS: $(ls /home/)"
          echo "SSH_DIR: $(ls -la ~/.ssh/ 2>/dev/null || echo 'no .ssh')"
