name: Fix SSH + Deploy Production
on: workflow_dispatch
jobs:
  fix:
    runs-on: self-hosted
    steps:
      - run: |
          set -e
          echo "=== 1/4 Restart SSH ==="
          systemctl restart sshd || service ssh restart || echo "sshd restart attempted"
          echo "=== 2/4 Write .env ==="
          PW1=$(openssl rand -base64 32 | tr -dc 'a-zA-Z0-9' | head -c 32)
          PW2=$(openssl rand -base64 32 | tr -dc 'a-zA-Z0-9' | head -c 32)
          JWT=$(openssl rand -hex 64)
          NWK=$(openssl rand -hex 32)
          mkdir -p /srv/ag
          cat > /srv/ag/.env << 'ENVEOF'
DOMAIN=advadiityagade.com
ACME_EMAIL=rajkhemani24@gmail.com
POSTGRES_USER=agadmin
POSTGRES_PASSWORD=PW1_PLACEHOLDER
POSTGRES_DB=agdb
REDIS_PASSWORD=PW2_PLACEHOLDER
LLM_BASE_URL=https://api.groq.com/openai/v1
LLM_MODEL_NAME=llama-3.3-70b-versatile
LLM_MOCK_MODE=true
SUPABASE_URL=https://kglqdihggncxkwwsgguw.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImtnbHFkaWhnZ25jeGt3d3NnZ3V3Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3Nzc0MDEzMDUsImV4cCI6MjA5Mjk3NzMwNX0.fFnHP2GAZcXEDpeI58uxYYwO5UliQFKG65I5plX01MA
JWT_SECRET=JWT_PLACEHOLDER
N8N_WEBHOOK_KEY=NWK_PLACEHOLDER
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD_HASH=admin
ENVIRONMENT=production
IMAGE_TAG=latest
NESL_MOCK_DELAY_SEC=3
ENVEOF
          sed -i "s/PW1_PLACEHOLDER/$PW1/g; s/PW2_PLACEHOLDER/$PW2/g; s/JWT_PLACEHOLDER/$JWT/g; s/NWK_PLACEHOLDER/$NWK/g" /srv/ag/.env
          chmod 600 /srv/ag/.env
          echo ".env written"
          
          echo "=== 3/4 Import SSH key ==="
          mkdir -p /root/.ssh
          echo "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIFBdZMtB65G0IIVz3MGIA4LeelHHo0zSOq7P5BdnoW6I luxor9@luxor9-Victus-by-HP-Laptop-16-d0xxx" >> /root/.ssh/authorized_keys
          chmod 600 /root/.ssh/authorized_keys
          echo "SSH key added to root"
          
          echo "=== 4/4 Verify ==="
          docker compose -f /home/luxor9/AGASSOCIATES/docker-compose.prod.yml config 2>&1 | head -5
          echo "=== DONE ==="
